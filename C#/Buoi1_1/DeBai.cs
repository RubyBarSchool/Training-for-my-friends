using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Buoi1_1
{
    class DeBai
    {
        /*
         * Bài 1: thiết kế hàm add kiểu dữ liệu trả về là void. Thiết kế hàm main nhập 2 số a,b từ bàn phím
         * sau đó tính kết quả dựa vào hàm add kia rồi in ra màn hình( lưu ý không được phép in tại hàm add mà phải in
         * kết quả tại hàm main)
         * 
         * Bài 2: Tạo hàm squart chuyền 2 số a,b nhập từ bàn phím và đổi giá trị của 2 số đó
         * ví dụ: hàm main nhập a = 2,b = 4; sau khi đi qua hàm squart thì a = 4, b = 2; sau đó in ra màn hình kết quả
         * 
         * Bài 3: ( Hơi nâng cao ):  
         *  trong hàm main chỉ gọi hàm calculator
         *  thiết kế code logic trong hàm calculator sao cho yêu cầu người dùng nhâpf 2 số bất kì 
         *  sau khi nhập xong thì show cho họ list sự lựa chọn cho người dùng chọn
         *  1: add
         *  2: sub
         *  3: mul
         *  4: div
         *  sau khi người dùng chọn xong thì in ra kết quả cho người dùng
         *  và hỏi người dùng có muốn tiếp tục k, nếu ấn y thì cho người dùng nhập lại 2 số rồi tương tự như phần trên
         *  nếu ấn n thì kết thúc chương trình và in ra ( cảm ơn quý khách đã sử dụng dịch vụ )
         *
         *mọi bài tập không cần check điều kiện mọi thứ người dùng nhập mặc định là đúng định dạng và nằm trong giới hạn 
         *đề bài yêu cầu ( vì chưa cần quá cao siêu nhẹ nhàng thôi =]]]]] )
         */
        // hàm void add
        // hàm void sub
        // hàm void mul
        // hàm void div
        // hàm void calculator
        //static void Main(string[] args)
        //{
        //    // chỉ gọi hàm calculator không viết bất kì code j vào đây nữa
        //}
        
    }
}
